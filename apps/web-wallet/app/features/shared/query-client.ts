// The QueryClient is owned by @agicash/wallet-sdk and re-exported here so the app's
// getQueryClient() call sites stay unchanged. Removed in the import-cleanup PR.
export { getQueryClient } from '@agicash/wallet-sdk/query-client';
