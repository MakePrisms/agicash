diff --git a/build/cjs/client/hydratedRouter.js b/build/cjs/client/hydratedRouter.js
index 191850ac4f84ce1172812d150a56b7273b002a45..8fba9ce5efd341c6284ee3ab72710d79ba5f7898 100644
--- a/build/cjs/client/hydratedRouter.js
+++ b/build/cjs/client/hydratedRouter.js
@@ -49,7 +49,13 @@ function instrumentHydratedRouter() {
         router.navigate = function sentryPatchedNavigate(...args) {
           // Skip if instrumentation API is enabled (it handles navigation spans itself)
           if (!createClientInstrumentation.isClientInstrumentationApiUsed()) {
-            maybeCreateNavigationTransaction(String(args[0]) || '<unknown route>', 'url');
+            const navTarget = args[0];
+            const name = typeof navTarget === 'string'
+              ? navTarget
+              : typeof navTarget === 'number'
+                ? `go(${navTarget})`
+                : (navTarget && navTarget.pathname) || '<unknown route>';
+            maybeCreateNavigationTransaction(name || '<unknown route>', 'url');
           }
           return originalNav(...args);
         };
diff --git a/build/esm/client/hydratedRouter.js b/build/esm/client/hydratedRouter.js
index 14dfab9432509b33f46b057a4d90e8dcf90a6e4d..0534a64f4888a18dc26d896911e835137f2845f1 100644
--- a/build/esm/client/hydratedRouter.js
+++ b/build/esm/client/hydratedRouter.js
@@ -47,7 +47,13 @@ function instrumentHydratedRouter() {
         router.navigate = function sentryPatchedNavigate(...args) {
           // Skip if instrumentation API is enabled (it handles navigation spans itself)
           if (!isClientInstrumentationApiUsed()) {
-            maybeCreateNavigationTransaction(String(args[0]) || '<unknown route>', 'url');
+            const navTarget = args[0];
+            const name = typeof navTarget === 'string'
+              ? navTarget
+              : typeof navTarget === 'number'
+                ? `go(${navTarget})`
+                : (navTarget && navTarget.pathname) || '<unknown route>';
+            maybeCreateNavigationTransaction(name || '<unknown route>', 'url');
           }
           return originalNav(...args);
         };
